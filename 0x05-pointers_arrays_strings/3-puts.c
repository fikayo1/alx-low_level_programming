#include "main.h"
/**
 * _puts - prints a string followed by new line
 * @str: a pointer to a string
 *
 */
void _puts(char *str)
{
int i = o;

while (str[i] != '\0')
{
_putchar(str[i]);
i++
}
_putchar('\n');
}
