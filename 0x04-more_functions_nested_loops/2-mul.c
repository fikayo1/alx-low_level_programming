#include "main.h"

/**
 * mul - multiplies two numbers
 *@a: first number
 *@b: seond number
 *
 *Return: the result
 */

int mul(int a, int b)
{
int result;
result = a * b;
_putchar(result + '0')
return(0);
}
