#include <stdio.h>
#include <string.h>
/**
 * main - prints out the alphabets in lowercase
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
  char alp[27];

  strcpy(alp, "a..z");
  putchar(alp);
  return(0);
}
