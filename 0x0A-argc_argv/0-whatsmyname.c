#include <stdio.h>
/**
 * main - prints its name followed by a new line
 * @argc: the numbers of arguments
 * @:argv: the argument vector
 * Return: 0 on success
 */
int main(argc, *argv[])
{
printf(argv[0]);
printf("\n");
}
