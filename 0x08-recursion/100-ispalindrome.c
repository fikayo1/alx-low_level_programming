#include "main.h"
/**
 * is_palindrome - checkes if a string is a palindrome
 * @s: a pointer to the string to be checked
 * Return: 1 if it a palindrome and 0 if it isn't
 */
int is_palindrome(char *s)
{
if (s == ' ')
{
return (1);
}

}
