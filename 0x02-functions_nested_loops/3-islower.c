#include "main.h"
/**
 * _islower - check if the character is lowercase
 *
 * Return: (1) if it is lowercase and (0) if it is not
 */

int _islower(int c)
{
if (islower(c) == 0)
return (1);
else
return (0);
}
