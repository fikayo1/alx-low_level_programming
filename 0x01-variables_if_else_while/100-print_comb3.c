#include <stdio.h>

/**
 * main - single digit
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
  int a = 0;
while (a < 100)
{
putchar(a + '0');
putchar(',');
putchar(' ');
}
a++;
}
(return (0);
}
