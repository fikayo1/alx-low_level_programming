#include <stdio.h>
#include <string.h>
/**
 *mian Entry point
 *
 *Return: Always 0 (Success)
 */


int main(void)
{
char str[150];

strcpy(str, "\"Programming is like building a multilingual puzzle");

puts(str);
return (0);
}
